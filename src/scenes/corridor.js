// src/scenes/corridor.js — long corridor, kid running ahead. Android waits.
// All behavior inherited from BaseStoryScene.

window.CorridorScene = class CorridorScene extends window.BaseStoryScene {
    constructor() {
        super('corridor');
    }
};
