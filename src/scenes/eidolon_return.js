// src/scenes/eidolon_return.js — the android walks away from the camera toward
// a building. Caption-only resolution before the loop restarts.
// All behavior inherited from BaseStoryScene.

window.EidolonReturnScene = class EidolonReturnScene extends window.BaseStoryScene {
    constructor() {
        super('eidolon_return');
    }
};
