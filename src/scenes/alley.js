// src/scenes/alley.js — first playable scene. Has Android sprite + Ink dialogue.

window.AlleyScene = class AlleyScene extends window.BaseStoryScene {
    constructor() {
        super('alley');
    }
};

// (Behavior is identical to base.)